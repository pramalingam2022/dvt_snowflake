name: pre-commit-check

on:
  pull_request:
    branches: 
      - dev

env:
  DBT_PROFILES_DIR: ./
  DBT_SNOWFLAKE_USERNAME: "praveenawsusohio"
  DBT_SNOWFLAKE_PW: gK43FL*Wxk6xN3zL
  DBT_SNOWFLAKE_ROLE: "accountadmin"

jobs:
  schedule_dbt_run:

    name: schedule_dbt_run
    runs-on: ubuntu-latest

    steps:

      - name: Check out
        uses: actions/checkout@master

      - uses: actions/setup-python@v1
        with:
        python-version: "3.7.x"

      - name: Install dependencies
        run: |
          pip install dbt
          dbt deps

    # dbt related commands here - run use --target prod/dev to run for specific environments
      - name: Run dbt models
        run: dbt run